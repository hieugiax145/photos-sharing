/**
 * fetchModel - Fetch a model from the web server.
 *
 * @param {} url      The URL to issue the GET request.
 *
 */
function fetchModel(url) {
  const models = null;
  return models;
}

export default fetchModel;
